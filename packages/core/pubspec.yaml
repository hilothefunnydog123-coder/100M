name: jobwalk_core
description: >-
  Shared model for Jobwalk: quotes, line items, the deterministic pricing
  math, AI draft parsing, and the customer-facing quote. Pure Dart, used by
  the Flutter app, the server, and the accuracy harness.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  image: ^4.10.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
